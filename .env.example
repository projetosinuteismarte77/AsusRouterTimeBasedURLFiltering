# Environment Configuration Template
# Copy this file to .env and fill in your actual values
# Then run: source .env

# Router IP address (default: 192.168.0.1)
export ROUTER_IP="192.168.0.1"

# Router admin username (default: admin)
export ROUTER_USERNAME="admin"

# Router admin password (REQUIRED - replace with your actual password)
export ROUTER_PASSWORD="your_router_password_here"
